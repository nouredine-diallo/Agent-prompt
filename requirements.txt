# --- Core IA & Moteur de Recherche ---
sentence-transformers>=2.2.2  # Pour les embeddings (all-MiniLM) et le Cross-Encoder
chromadb>=0.3.30              # Base de données vectorielle

# --- Traitement des Données & Maths ---
numpy>=1.24.0                 # Utilisé pour le tri des tenseurs (argsort) dans le reranker
pandas>=2.0.0                 # Utilisé pour les scripts d'évaluation (benchmarks)
nltk>=3.8.1                   # Utilisé pour le chunking (sent_tokenize)

# --- Appels API & Environnement ---
requests>=2.28.0              # Appels HTTP natifs vers l'API Groq/LLMs
python-dotenv>=1.0.0          # Gestion des clés d'API (.env)

# --- UI & Serveur ---
streamlit>=1.24.0             # Interface utilisateur de démo
fastapi>=0.95.0               
uvicorn[standard]>=0.22.0    

# --- Qualité & Tests ---
jsonschema>=4.0.0             # Validation des structures JSON
pytest>=7.0.0                 # Tests unitaires